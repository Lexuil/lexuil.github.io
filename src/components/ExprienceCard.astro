---
import { Icon } from "astro-icon/components";

interface Props {
  iconName: string;
  title: string;
  experience: string;
  bgColor: string;
}
const { iconName, title, experience, bgColor } = Astro.props;
---

<li
  class:list={[
    "flex flex-col items-center justify-center rounded-lg w-60 h-60",
    `bg-${bgColor}/90`,
  ]}
>
  <Icon name={iconName} class="w-28 h-28" />
  <span class="text-2xl font-semibold">{title}</span>
  <span>{experience}</span>
</li>
