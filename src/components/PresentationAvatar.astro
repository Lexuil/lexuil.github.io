---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";
interface Props {
  imageUrl: ImageMetadata;
}
const { imageUrl } = Astro.props;
---

<Image
  src={imageUrl}
  alt="Foto de Oscar Umaña"
  class="rounded-full w-44 h-44 rotate-[20deg]"
  loading={"eager"}
/>
